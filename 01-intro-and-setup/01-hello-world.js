//Cài extension
//Để save nhấn ctrl+S


console.log('Xin chào Việt Nam');
console.log('Xin chào Việt Nam2');

// Users/hoanghuyentrang/Documents/202603-PW-JS-TS/
// 01-intro-and-setup/01-hello-world.js
// cd
// Khi mở terminal, thư mục hiện tại sẽ là thư mục gốc
// Gõ 'cd' rồi nhấn 'Tab' để tự động đi vào thư mục cần chạy
// 'ls' = list , dùng để hiển thị các file trong thư mục hiện tại 
// Gõ 'node <ten-file>.js' để chạy file JavaScript
// Extension đã thiết lập sẵn môi trường Node.js


// Command +/ để comment hoặc bỏ comment code (trên window thì bấm Ctrl +/)


// Công dụng của biến là để lưu trữ dữ liệu (giá trị) để máy tính có thể nhớ và sử dụng sau này
// Cách khai báo biến:
// Cú pháp là '[từ khoá][tên biến]=[giá trị]'
// A.'let' (chiếc hộp linh hoạt)
// B. Const (chiếc hộp niêm phong)
// Viết tắt của 'constant' (hằng số)
// Dùng khi bạn muốn giá trị này không bao giờ thay đổi sau khi khai báo
// Chỉ dùng 'let' để khai báo biến lần đầu, những lần sau chỉ cần gọi tên biến, không cần dùng lại 'let'
// Dùng khi bạn biết giá trị của biến có thể thay đổi trong tương lai
// dùng console.log('tên biến') để in giá trị của biến ra màn hinh, giúp kiểm tra và debug.

// Các quy tắc đặt tên biến:
// 1. ký tự đặc biệt, dấu gạch ngang là ko được, chỉ dùng dấu gạch dưới
// 2. ko được đặt số ở đầu
// 3. ko có khoảng trắng
// 4. Ko có từ khoá của javascript ('reserved keyword') vd if, function, return,...

// Quy tắc ngầm, naming convention
// Vi phạm cái này code vẫn chạy nhưng đánh giá là nghiệp dư
// Vd: let thongtinhocvien = 'abc'

// 'snake_case'let thong_tin_hoc_vien = 'abc'
// Tạm chấp nhận được, nhưng thường dùng nhiều hơn Python/SQL.
// 'PascalCase': let ThongTinHocVien = 'abc
// 'CamelCase' (Chuẩn trong JS/TS): let thongTinHocVien = 'abc'
// Đây là cách viết chuẩn


